<div class='quadro'>
<% if current_usuario.admin? %>
  <fieldset class='quadros'>
    <legend>Clientes <span class="usuario"></span></legend>
      <h3> Informações de Clientes</h3>
      <p>Clientes Cadastrados: <%= Cliente.all.count %></p>
      <p>Clientes Bloqueados: <%= Cliente.where(bloqueado: true).count %></p>
      <p>Clientes Cadastrados hoje: <%= Cliente.where(created_at: Time.now.beginning_of_day).count %></p>
  </fieldset>

<fieldset class='quadros'>
    <legend>Pedidos<span class="pedido"></span></legend>
    
  </fieldset>

<fieldset class='quadros'>
    <legend>Estoque<span class="estoque"></span></legend>
      <h3> Informações de Estoque <%= link_to 'Ver tudo', home_relatorio_produtos_path %></h3>
      <table class='tabela'>
        <thead>
          <tr>
            <th>Acabando</th>
          </tr>
        </thead>
        <tbody>
          <% Produto.acabando.each do |produto| %>
            <tr>
              <td><%= produto.nome %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <table class='tabela'>
        <thead>
          <tr>
            <th>Em falta</th>
          </tr>
        </thead>
        <tbody>
          <% Produto.zerados.each do |produto| %>
            <tr>
              <td><%= produto.nome %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
  </fieldset>

<fieldset class='quadros'>
    <legend>Vendas<span class="venda"></span></legend>
      <h3> Informações de Clientes</h3>
      <p>Valor vendido hoje: R$ <%= exibir_decimal(Pedido.vendidos_hoje) %></p>
      <p>Vendas do mês: R$<%= exibir_decimal(Pedido.vendas_do_mes) %></p>    
  </fieldset>
  <% else %>
<fieldset class='quadros'>
    <legend>Conta<span class="venda"></span></legend>
      <h3> Informações da Conta</h3>
      <p>Quantidade de pedidos:  current_usuario.pedidos.count %></p>
      <p>Valor a pagar: R$150,00 %></p>    
  </fieldset>

<% end %>
</div>