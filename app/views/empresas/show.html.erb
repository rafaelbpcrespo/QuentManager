<%- model_class = Empresa -%>
<div class="page-header">
  <h1 class='titulo'><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
  <%= link_to t('.nova_empresa', :default => t("helpers.links.nova_empresa")),
            new_empresa_path,
            :class => 'button blue-cadastrar' %>
</div>

<fieldset class='show-empresas'>
  <legend>Dados Cadastrais</legend>
  <strong><%= model_class.human_attribute_name(:nome) %>:</strong>
  <%= @empresa.nome %><br/>
  <strong><%= model_class.human_attribute_name(:telefone) %>:</strong>
  <%= @empresa.telefone %><br/>
  <strong>Endereço:</strong>
  <%= @empresa.endereco %><br/>
  <strong>Observação:</strong>
  <%= @empresa.observacao %>
</fieldset>

<div class="form-actions">
  <%= link_to t('.voltar', :default => t("helpers.links.voltar")),
              empresas_path, :class => 'btn'  %>
  <%= link_to edit_empresa_path(@empresa), :class => 'button blue' do %>
                      <span class="editar"></span>          
          <% end %>

  <%= link_to empresa_path(@empresa), :method => 'delete', :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Deseja excluir esta empresa?')) }, :class => 'button red' do %>
                        <span class="remover"></span>
                      <% end %>

</div>
