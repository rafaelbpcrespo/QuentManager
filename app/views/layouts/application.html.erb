<!DOCTYPE html>
<html>
<head>
  <title>QuentManager</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => false %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => false %>
  <%= csrf_meta_tags %>
         <p class="notice"><%= notice %></p>
         <p class="alert"><%= alert %></p>
</head>
<body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">QuentManager</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <li><%= link_to "Clientes", "/clientes"  %></li>
              <li><%= link_to "Pedidos", "/pedidos"  %></li>
              <li><%= link_to "Produtos", "/produtos"  %></li>
              <li><% if usuario_signed_in? %>
    Logado como <%= current_usuario.email %>. Não é você?
    <%= link_to "Sair", destroy_usuario_session_path, :class => 'btn btn-link btn-xs', :method => :delete %></li>
  <% else %>
    <li><%= link_to "Cadastrar", new_usuario_registration_path, :class => 'btn btn-link btn-xs' %> </li> 
    <li><%= link_to "Entrar", new_usuario_session_path, :class => 'btn btn-link btn-xs' %></li>
  <% end %>
            </ul>
          </div><!--/.nav-collapse -->
          <div id="user_nav">
  <% if usuario_signed_in? %>
    Logado como <%= current_usuario.email %>. Não é você?
    <%= link_to "Sair", destroy_usuario_session_path, :method => :delete %>
  <% else %>
    <%= link_to "Cadastrar", new_usuario_registration_path %> ou <%= link_to "Entrar", new_usuario_session_path %>
  <% end %>
</div>
        </div>
      </div>
    </div>


<div class="container">
  <div class="row">
      <div class="span3">
        <h2>Resumo</h2>
          <p> Quantidade de clientes: </p><%= @qtd_clientes %>
        </div>
      <div class="span8"><%= yield %></div>
  </div>
</div>

</body>
</html>
